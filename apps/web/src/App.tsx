// App.tsx is no longer the entrypoint; main.tsx handles bootstrap and routing.
// This file is kept as a placeholder that re-exports the routes config in case
// a consumer ever needs it.
export { routes } from '@/routes/routes';
